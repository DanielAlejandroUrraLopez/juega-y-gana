import React, {Component} from 'react';
import Ruleta from './containers/ruleta';

class Init extends Component {
  render() {
    return (
      <>
        <Ruleta />
      </>
    );
  }
}
export default Init;
